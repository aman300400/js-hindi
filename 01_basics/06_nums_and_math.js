const score =400;
console.log(score);

const balance = new Number(100)
console.log(balance);

console.log(balance.toString()) ;// string have additional properties  check on goggle
console.log(balance.toFixed(2)); means how much  numbers you want after decimal for example 2 means it print upto two decimal points 100.00 if it is  one decimal point it meanas  100.0 upto one decimal  point


const otherNumber= 23.8966;
console.log(otherNumber.toPrecision(3)); // matlab haama precise value chahiya three digit taaak isko bohot dhyan saa use kaarna a hooga nahi to dikkat ho jaaaogii

const hundreds = 1000000
console.log(hundreds.toLocaleString("en-IN"));

+++++++++++++++++++++++++++++++++++++++++maths++++++++++++++++++++++++++++++++++++++++++++++

console. log (Math) ;
console. log(Math.abs(-4)) ;
console. log(Math.round(4.6)) ;
console. log(Math.ceil(4.2));
console. log(Math.floor(4.9)) ;
console. log(Math.min(4, 3, 6, 8) ) ;
console. log(Math.max(4, 3, 6, 8));

console.log(math,ranodm());//for making a game suppose a dice game where we need  to make dice game
console.log((math.random()*10)+1);

const min =10;
const max =20; // time stamp  2:46:52

console.log(math.floor(math.random()*(max-min+1))+min)//  very inportant formula ha yaa keep it in mind it will help you in many things like making a game or any other thing where you need to generate random number between two numbers
// for example if you want to generate a random number between 10 and 20 then you can use this formula it will give you a random number between 10 and 20 